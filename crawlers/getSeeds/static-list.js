// WHEN TO USE:
// The seed list is small and fixed. Equivalent to setting Seed URLs in
// Configuration, but useful when the hook must exist for documentation purposes.

function getSeeds() {
  return [
    "https://www.example.com/opinions/current",
    "https://www.example.com/opinions/archived",
  ];
}
